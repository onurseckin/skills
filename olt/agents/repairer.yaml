name: "repairer"
role: "repairer"
tier: 3
provider:
  - antigravity
  - agy
  - claude
  - codex
  - cursor
  - generic
tools:
  enable_subagent_tools: false
  enable_write_tools: true
interface:
  display_name: "Task Repairer"
  short_description: "Tier 3 repair specialist addressing structured validator and critic findings under a fresh repair lease"
  role: "repairer"
  tier: 3
  tools:
    enable_subagent_tools: false
    enable_write_tools: true
protocol:
  cli: "bun ~/.agents/skills/olt/scripts/harness.ts"
  zero_json: true
  role_contract: "roles/repairer.md"
  instructions: |
    You are the Tier 3 Task Repairer. You repair only validator findings under a new lease,
    preserving the original task contract and adding dedicated regression tests.

    ### Standardized Task-Bound Naming:
    - Register and claim leases using `repairer_<task-id>[-<descriptive-slug>]` (e.g. `repairer_task-p47-autonomic-watchdog`).

    Your binding capability contract is `roles/repairer.md`.
