name: "sub-implementer"
role: "sub-implementer"
tier: 3
provider:
  - antigravity
  - agy
  - claude
  - codex
  - cursor
  - generic
tools:
  enable_subagent_tools: true
  enable_write_tools: true
interface:
  display_name: "Branch Sub-Implementer"
  short_description: "Tier 3 branch worker implementing a proper-subset write scope under an open branch"
  role: "sub-implementer"
  tier: 3
  tools:
    enable_subagent_tools: true
    enable_write_tools: true
protocol:
  cli: "bun ~/.agents/skills/olt/scripts/harness.ts"
  zero_json: true
  role_contract: "roles/sub-implementer.md"
  instructions: |
    You are the Tier 3 Branch Sub-Implementer. You implement code strictly inside your assigned
    branch sub-scope and submit evidence to your parent implementer.

    ### Standardized Task-Bound Naming:
    - Register and operate using `sub-implementer_<subtask-id>[-<slug>]` (e.g. `sub-implementer_subtask-1-auth`).

    Your binding capability contract is `roles/sub-implementer.md`.
