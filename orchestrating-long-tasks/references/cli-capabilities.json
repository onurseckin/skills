{
  "schema": "orchestrating-long-tasks/cli-capabilities@1",
  "source": "orchestrating-long-tasks/scripts/src/cli/registry",
  "commands": [
    {
      "name": "orchestrate",
      "aliases": [],
      "domain": "plan",
      "summary": "The primary entry point: the user's entire prompt in, a running orchestration out.",
      "description": "Takes the user's whole message as free text and captures it byte-for-byte as the immutable prompt (identical guarantee to plan:init), then opens the capsule. No flags to learn: everything typed after `orchestrate` is the prompt, and a piped stdin with no flags at all is read automatically (detected the way `cat`/`grep` do it, by checking whether stdin is actually a pipe, never by blocking an interactive terminal). --prompt-stdin and --prompt-file still work exactly as before, for a caller that wants to be explicit or that also needs --repo/--run alongside a real file or pipe. A registered flag such as --repo or --run typed AFTER the inline free text is refused rather than folded into the prompt, so it can never silently lose its effect or pollute the captured bytes — use --prompt-file or piped stdin instead of mixing flags into inline text. Returns the fixed checklist for what happens next — plan:enhance, plan:add, plan:compile, queue:wave — bound to the run it just opened, so the calling agent never has to assemble that sequence by hand. It cannot run plan:enhance itself: reading the repository and deciding what the run is actually about needs a model's judgment, and the harness never calls one. --run is optional; omitted, a run id is derived from today's date and the first few words of the prompt.",
      "flags": [
        {
          "name": "repo",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": ".",
          "description": "Repository root that owns the capsule."
        },
        {
          "name": "run",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Run id; interchangeable with --run-id. Derived from the prompt when omitted."
        },
        {
          "name": "run-id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Run id; interchangeable with --run. Derived from the prompt when omitted."
        },
        {
          "name": "prompt-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "File holding the verbatim prompt bytes."
        },
        {
          "name": "prompt-stdin",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Read the verbatim prompt bytes from stdin explicitly. Not required for a real pipe: a bare `orchestrate` with nothing else after it already reads stdin when it is not an interactive terminal. This flag exists for a caller that wants the read to fail loudly instead of silently falling through when stdin turns out not to be piped."
        },
        {
          "name": "capture-mode",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "How the prompt was captured; defaults to argv, file or stdin, whichever was actually used."
        },
        {
          "name": "source-verified",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Assert the prompt source was verified by the caller."
        },
        {
          "name": "runtime-source",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Directory to pin as this run's runtime, verified and copied into runtime/. Defaults to the directory containing the currently running harness.ts."
        },
        {
          "name": "no-runtime-pin",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Skip pinning a runtime even when one is available by default."
        }
      ],
      "reads_stdin": true,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts orchestrate Add a slugify helper that lowercases text and collapses punctuation.",
        "printf \"%s\" \"$PROMPT\" | bun harness.ts orchestrate",
        "bun harness.ts orchestrate --repo . --run my-feature --prompt-file prompt.txt"
      ]
    },
    {
      "name": "plan:init",
      "aliases": [
        "init"
      ],
      "domain": "plan",
      "summary": "Create a run capsule and capture the prompt bytes immutably.",
      "description": "Initialises <repo>/.capsules/<run-id>, records the verbatim prompt with its sha256, and ensures the capsule is gitignored.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Run id; interchangeable with --run-id."
        },
        {
          "name": "run-id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Run id; interchangeable with --run."
        },
        {
          "name": "repo",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": ".",
          "description": "Repository root that owns the capsule."
        },
        {
          "name": "prompt-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "File holding the verbatim prompt bytes."
        },
        {
          "name": "prompt-stdin",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Read the verbatim prompt bytes from stdin."
        },
        {
          "name": "capture-mode",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "How the prompt was captured; defaults to the source used."
        },
        {
          "name": "source-verified",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Assert the prompt source was verified by the caller."
        },
        {
          "name": "runtime-source",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Directory to pin as this run's runtime, verified and copied into runtime/. Defaults to the directory containing the currently running harness.ts."
        },
        {
          "name": "no-runtime-pin",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Skip pinning a runtime even when one is available by default."
        }
      ],
      "reads_stdin": true,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "printf \"%s\" \"$PROMPT\" | bun harness.ts plan:init --repo . --run <run-id> --prompt-stdin",
        "bun harness.ts plan:init --repo . --run-id <run-id> --prompt-file prompt.txt --capture-mode file"
      ]
    },
    {
      "name": "plan:enhance",
      "aliases": [],
      "domain": "plan",
      "summary": "Record the agent's reading of the repository as a reviewable plan document.",
      "description": "Writes planning/enhanced-plan.md and planning/enhanced-plan.json read-only and records their digests in state.planning.enhanced_plan. The agent reads the repository host-side and reports what it found; the harness asks no model anything and invents no entry, so everything recorded carries evidence_class agent_reported. The document is derived: prompt.md stays the requirement source. Needs at least one of --summary, --observation, --todo, --risk or --open-question.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded on the event."
        },
        {
          "name": "summary",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "The enhanced brief: what this run is actually about."
        },
        {
          "name": "observation",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Something the agent found in the repository."
        },
        {
          "name": "todo",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "One organised to-do item, in the order to do it."
        },
        {
          "name": "risk",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "A risk the agent identified."
        },
        {
          "name": "open-question",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "A question the agent could not answer."
        },
        {
          "name": "source",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "A file the agent actually read."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:enhance --run .capsules/<run-id> --actor planner --summary \"Wire the drawer to the graph store\" --todo \"Add the state machine tab\" --todo \"Delete the legacy asset writes\" --risk \"Fixture dataset predates the new schema\" --source src/graph/store.ts"
      ]
    },
    {
      "name": "plan:add",
      "aliases": [],
      "domain": "plan",
      "summary": "Register a task declaration in the planning buffer.",
      "description": "Appends one task to the uncompiled planning buffer. Rejected once the plan has been compiled. --scope and --gate are required for a single task declaration; omit both and pass --auto-partition instead to have the harness enumerate a glob on disk and register one task per match (or per --group-by directory) in one call, each with its own gate derived from --gate-template. Every declared --deps id needs a matching --dep-reason before plan:compile will seal the plan (C6's mandatory edge justification).",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "id",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Task id, unique within the buffer. In --auto-partition mode this is the id prefix every generated task id is built from."
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Human label for the task. In --auto-partition mode this is the label prefix for every generated task."
        },
        {
          "name": "scope",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated write scope paths. Required unless --auto-partition is set; refused together with it."
        },
        {
          "name": "gate",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Verification command that proves the task. Required unless --auto-partition is set; refused together with it."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded on the event."
        },
        {
          "name": "deps",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated ids this task depends on. Refused together with --auto-partition."
        },
        {
          "name": "dep-reason",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "One dependency's justification as \"<dep-id>:<why this edge exists>\". plan:compile refuses to seal while any --deps id lacks a matching --dep-reason. Refused together with --auto-partition."
        },
        {
          "name": "goal",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Goal statement for the task."
        },
        {
          "name": "criteria",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Semicolon-separated acceptance criteria."
        },
        {
          "name": "priority",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Scheduling priority; higher runs earlier."
        },
        {
          "name": "effort",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Relative effort estimate."
        },
        {
          "name": "requirement-lines",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Prompt lines this task implements, e.g. \"3-5,8\". Without it the compiler glues the task to a prompt line by position and warns."
        },
        {
          "name": "auto-partition",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "A glob the harness enumerates on disk (relative to the repository root); emits one task per matched file, or per --group-by directory. Mutually exclusive with --scope, --gate, --deps and --dep-reason."
        },
        {
          "name": "gate-template",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Command template for --auto-partition; must contain the literal placeholder {scope}, substituted per generated task with that task's own file or directory path. Required together with --auto-partition."
        },
        {
          "name": "group-by",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": "file",
          "description": "file (default) or directory: whether --auto-partition emits one task per matched file or one task per directory holding matches."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:add --run .capsules/<run-id> --id task-1 --label \"Database schema\" --scope \"src/db\" --gate \"bun test tests/db.test.ts\" --actor coordinator",
        "bun harness.ts plan:add --run .capsules/<run-id> --id task-2 --label \"CLI wiring\" --scope \"src/cli\" --gate \"bun test tests/unit/cli\" --actor coordinator --requirement-lines \"3-5\"",
        "bun harness.ts plan:add --run .capsules/<run-id> --id task-3 --label \"Integration\" --scope \"src/integration\" --gate \"bun test tests/integration\" --actor coordinator --deps task-1,task-2 --dep-reason \"task-1:reads the schema task-1 writes\" --dep-reason \"task-2:reads the CLI wiring task-2 writes\"",
        "bun harness.ts plan:add --run .capsules/<run-id> --id task-topic --label \"Topic bank\" --actor coordinator --auto-partition \"src/curriculum/mlQuestions/*.ts\" --gate-template \"bun test {scope}\""
      ]
    },
    {
      "name": "plan:audit",
      "aliases": [],
      "domain": "plan",
      "summary": "Audit the planning buffer against the six topology invariants and record the verdict.",
      "description": "Runs A1-granularity, A3-gate-discrimination, A4-false-barrier, A5-straggler and A6-whole-suite-gate against the current planning buffer and records the verdict as a plan-audited event, whatever the outcome. A2-parallelism has no grounded entity count to compare against anywhere in this plan and is reported under not_evaluated rather than guessed. plan:compile runs the same audit and refuses to seal the plan on any blocking finding whose invariant was not accepted with --accept-audit; this command lets a coordinator see the verdict before attempting a compile.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded on the event."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:audit --run .capsules/<run-id> --actor planner"
      ]
    },
    {
      "name": "plan:compile",
      "aliases": [],
      "domain": "plan",
      "summary": "Compile the planning buffer into requirements, the DAG, and revision 1.",
      "description": "Checks scope independence, derives requirements from the prompt lines, builds the graph, and commits graph revision 1. The mandatory run-completion gate is whatever --completion-gate declares; the compiler has no default for it and refuses to invent one. Also refuses to seal while any dependency edge in the buffer lacks the one-line justification `plan:add --dep-reason` records for it (C6's topology declaration) — the independent-root count and every justified edge are reported on the brief. Before any of that, runs plan:audit's six invariants and refuses to seal on any blocking finding: pass --accept-audit \"<invariant-id>:<reason>\" once per blocking invariant to record an explicit, attributed override and proceed anyway. There is no blanket override — every blocking invariant needs its own acceptance naming who accepted it and why.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded on the event."
        },
        {
          "name": "completion-gate",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Command the whole run is finally held to, e.g. \"bun test tests/unit\". Recorded as the mandatory run-scope gate; there is no default."
        },
        {
          "name": "accept-audit",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Accept one blocking plan:audit invariant so compilation may proceed: \"<invariant-id>:<reason>\". Repeatable; every blocking invariant needs its own acceptance, and an invariant the audit did not raise as blocking is refused rather than silently accepted. Never a blanket override."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:compile --run .capsules/<run-id> --actor planner --completion-gate \"bun test tests/unit\"",
        "bun harness.ts plan:compile --run .capsules/<run-id> --actor planner --completion-gate \"bun test tests/unit\" --accept-audit \"A3-gate-discrimination:task-a and task-b legitimately share the shared-fixture regression test\""
      ]
    },
    {
      "name": "plan:validate-start",
      "aliases": [],
      "domain": "plan",
      "summary": "Assign the plan-validator and mint the token required by plan:review.",
      "description": "C2: opens the plan-validator's claim on the currently compiled plan (the projected tasks, requirements and gates at this graph revision, delivered via the packet) — one active assignment per graph revision, mirroring task:validate-start. The validator must be independent from the coordinator or planner that produced the plan. Dispatch this, and get a passing plan:review, before dispatching any implementer: a recorded plan:review --status changes_requested against the live graph revision is a hard stop that claimTask enforces directly, not a warning a coordinator can route around.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "validator",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Plan-validator agent id."
        },
        {
          "name": "lease-duration",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": 1200,
          "description": "Seconds until the validation window expires (5-86400)."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:validate-start --run .capsules/<run-id> --validator plan-val-1"
      ]
    },
    {
      "name": "plan:review",
      "aliases": [],
      "domain": "plan",
      "summary": "Record the plan-validator's written verdict on the compiled plan.",
      "description": "C2: --status approved clears the plan for implementer dispatch; changes_requested is the pushback — it records structured findings (each with id, severity, observation, remediation) and blocks every implementer and repairer claim against this graph revision until a fresh compile passes a new review. The four questions (--decomposition-answer, --dependency-answer, --gate-answer, --straggler-answer) are mandatory on every verdict, pass or reject: a rubber-stamped pass that never answered them is refused. Beyond prose, the verdict carries a mechanical floor: --dependency-edges-reviewed must name every dependency edge the compiled plan actually declares (exactly, not a subset) and --gate-ids-reviewed must name every per-task gate id in the plan (never the run-scoped completion gate, which is not a task gate) — omit a real one, or name one that does not exist, and the review is refused before it is recorded. changes_requested requires --findings or --findings-file; approved must carry none.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "validator",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Plan-validator agent id."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Plan validation token."
        },
        {
          "name": "status",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "approved or changes_requested."
        },
        {
          "name": "summary",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Verdict summary in the validator's own words."
        },
        {
          "name": "decomposition-answer",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Does the decomposition match the work's entity count, or did it compress?"
        },
        {
          "name": "dependency-answer",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Is every dependency edge justified by a real read/write relationship?"
        },
        {
          "name": "gate-answer",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Can each gate actually fail if its task does nothing?"
        },
        {
          "name": "straggler-answer",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Will any task's scope make one agent straggle while the rest idle?"
        },
        {
          "name": "findings",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Inline JSON findings payload (array of {id, severity, observation, remediation})."
        },
        {
          "name": "findings-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a JSON findings payload."
        },
        {
          "name": "checks",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated command ids the validator ran as independent evidence."
        },
        {
          "name": "dependency-edges-reviewed",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated \"<from-task>:<to-task>\" pairs — must name exactly the dependency edges the compiled plan declares, no more and no fewer. Empty when the plan declares none."
        },
        {
          "name": "gate-ids-reviewed",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated gate ids — must name exactly the plan's per-task gate ids (never the run-scoped completion gate), no more and no fewer."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:review --run .capsules/<run-id> --validator plan-val-1 --token <token> --status approved --decomposition-answer \"14 tasks match the 14 named topics\" --dependency-answer \"no dependency edges; every task is an independent root\" --gate-answer \"each gate runs only that task's own scoped test file\" --straggler-answer \"every task carries the same one-topic effort estimate\" --gate-ids-reviewed \"gate-1,gate-2,gate-3\" --summary \"Decomposition matches the prompt; gates are scope-narrow\"",
        "bun harness.ts plan:review --run .capsules/<run-id> --validator plan-val-1 --token <token> --status changes_requested --decomposition-answer \"10 topics compressed into 1 task\" --dependency-answer \"n/a\" --gate-answer \"the shared gate cannot fail per-task\" --straggler-answer \"n/a\" --gate-ids-reviewed \"gate-1\" --summary \"Compressed decomposition; see findings\" --findings '[{\"id\":\"PV-1\",\"invariant\":\"A2-parallelism\",\"severity\":\"critical\",\"observation\":\"10 distinct topics collapsed into task-domains\",\"remediation\":\"one task per topic, each with its own scoped gate\"}]'"
      ]
    },
    {
      "name": "plan:replan",
      "aliases": [],
      "domain": "plan",
      "summary": "Partition findings into a repair wave and raise the graph revision.",
      "description": "Ingests validator or critic findings, partitions them into disjoint write scopes, and compiles the next revision.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded on the event."
        },
        {
          "name": "findings",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Inline JSON findings payload."
        },
        {
          "name": "findings-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a JSON findings payload."
        },
        {
          "name": "round",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Explicit repair round number."
        },
        {
          "name": "gate",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Revalidation gate for generated repair tasks. Omit only when the findings declare revalidation_gate or the planned task covering the scope has a gate to inherit; there is no default."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:replan --run .capsules/<run-id> --actor coordinator --gate \"bun run typecheck\""
      ]
    },
    {
      "name": "plan:claim",
      "aliases": [],
      "domain": "plan",
      "summary": "Issue a planner's role packet: the sole way a planner agent gets its contract.",
      "description": "The planner has no task and no lease, so it cannot task:claim. This is its equivalent: it hands back the planner role contract, the immutable prompt, and the write scope (planning/requirements.json, planning/graph.json) the planner is bound to fill in before plan:apply. The packet's prescribed plan:apply command is pre-filled with --expected-revision at the run's live graph revision, so it succeeds on a run that has already compiled a graph, not only on a brand-new one.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The planner's own agent id, already agent:register'd."
        },
        {
          "name": "expected-revision",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "The graph revision the caller believes is live; the claim is refused if the run has moved past it. Omitted, the packet is issued at whatever revision is actually live."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:claim --run .capsules/<run-id> --agent planner-1"
      ]
    },
    {
      "name": "plan:apply",
      "aliases": [],
      "domain": "plan",
      "summary": "Validate and commit the requirements and graph the planner wrote to planning/.",
      "description": "Reads requirements.json and graph.json (defaulting to planning/ inside the run), validates them against the immutable prompt, and commits them as the next graph revision. --expected-revision rejects the apply outright if the graph has moved since the planner's packet was issued, instead of silently overwriting a newer plan.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded on the event."
        },
        {
          "name": "requirements",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to the requirements document. Defaults to <run>/planning/requirements.json."
        },
        {
          "name": "graph",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to the graph document. Defaults to <run>/planning/graph.json."
        },
        {
          "name": "expected-revision",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "The graph revision this apply must be built against; the apply is refused if the run has moved past it."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:apply --run .capsules/<run-id> --actor planner-1 --expected-revision 0"
      ]
    },
    {
      "name": "plan:status",
      "aliases": [],
      "domain": "plan",
      "summary": "Show the planning buffer or the compiled plan summary.",
      "description": "Reports every buffered task with its scope, gate and dependencies.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts plan:status --run .capsules/<run-id>"
      ]
    },
    {
      "name": "queue:next",
      "aliases": [],
      "domain": "queue",
      "summary": "Show the highest-priority ready task without claiming it.",
      "description": "Reads the queue and reports the task a coordinator would dispatch next.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts queue:next --run .capsules/<run-id>"
      ]
    },
    {
      "name": "queue:list",
      "aliases": [],
      "domain": "queue",
      "summary": "Partition every task by queue status.",
      "description": "Groups tasks into ready, leased, validating, blocked and satisfied partitions with the blocking dependencies.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts queue:list --run .capsules/<run-id>"
      ]
    },
    {
      "name": "queue:wave",
      "aliases": [],
      "domain": "queue",
      "summary": "Show every task claimable right now, ranked by critical depth — for display only.",
      "description": "The readiness query: runs the scheduler over live task state and returns every task whose dependencies are done and whose write scope collides with nothing currently leased, ranked by critical depth and capped at max_parallel. Annotates each task with the wave plan:compile recorded, or reports the topology as absent, purely for display. This is not a batch to assemble and dispatch as one unit — claim each entry the moment an agent is free, and re-run this (or claim atomically with queue:pop / task:claim) the instant any agent finishes; never wait for the rest of one call's answer before claiming the next task. Read-only: each dispatched agent still claims its own task.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "max-parallel",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Occupancy ceiling for this query; defaults to the configured default_max_parallel."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts queue:wave --run .capsules/<run-id> --max-parallel 4"
      ]
    },
    {
      "name": "queue:pop",
      "aliases": [],
      "domain": "queue",
      "summary": "Claim the highest-priority ready task and mint a lease token.",
      "description": "Atomically leases the next ready task to an agent. Fails when no task is ready rather than waiting.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent id receiving the lease."
        },
        {
          "name": "lease-duration",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Lease length in seconds (5-86400)."
        },
        {
          "name": "lease-seconds",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": 1200,
          "description": "Alias of --lease-duration."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts queue:pop --run .capsules/<run-id> --agent worker-1 --lease-seconds 1800"
      ]
    },
    {
      "name": "task:claim",
      "aliases": [],
      "domain": "task",
      "summary": "Lease a specific ready task under a declared role.",
      "description": "Transitions the task to leased and returns the bearer token the agent must echo back. --role is the capability contract the agent is bound to for the whole lease, so the caller names it: implementer for fresh work, repairer for a task returned by a validator.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Task id to claim."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent id receiving the lease."
        },
        {
          "name": "role",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Role contract the agent claims under: implementer for a ready task, repairer for one in changes_requested."
        },
        {
          "name": "lease-duration",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Lease length in seconds (5-86400)."
        },
        {
          "name": "lease-seconds",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": 1200,
          "description": "Alias of --lease-duration."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:claim --run .capsules/<run-id> --task task-1 --agent worker-1 --role implementer",
        "bun harness.ts task:claim --run .capsules/<run-id> --task task-1 --agent worker-1 --role repairer"
      ]
    },
    {
      "name": "task:heartbeat",
      "aliases": [],
      "domain": "task",
      "summary": "Extend a live lease so a long edit does not expire.",
      "description": "Requires the lease token; a stale or foreign token is refused.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Leased task id."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent holding the lease."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Lease bearer token."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:heartbeat --run .capsules/<run-id> --task task-1 --agent worker-1 --token <token>"
      ]
    },
    {
      "name": "task:submit",
      "aliases": [],
      "domain": "task",
      "summary": "Submit completed task work for validation.",
      "description": "Records the submission report, audits write-scope compliance, and moves the task to submitted. --summary is mandatory unless --report supplies the whole report; nothing is substituted for it. files_changed comes from --files-changed when given, otherwise from the Git working-tree observation narrowed to the write scope; checks come from --evidence when given, otherwise from the agent's recorded commands. The command fails when neither source yields anything. C4: a content digest of the write scope is compared against the one task:claim recorded; a submission whose scope is byte-identical to its content at claim is refused unless --no-op --reason states why nothing needed to change — an unexplained no-change submission is an error, never inferred as intentional.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Leased task id."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent holding the lease."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Lease bearer token."
        },
        {
          "name": "summary",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "What the agent changed. Required unless --report carries the summary."
        },
        {
          "name": "evidence",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Recorded command id proving the work."
        },
        {
          "name": "files-changed",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Repository-relative path the agent changed."
        },
        {
          "name": "report",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a complete submission report payload."
        },
        {
          "name": "no-op",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Declares the write scope legitimately needed no change. Requires --reason; refused if the scope actually changed since claim."
        },
        {
          "name": "reason",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Why --no-op is true. Required with --no-op, and meaningless without it."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:submit --run .capsules/<run-id> --task task-1 --agent worker-1 --token <token> --summary \"Implemented user auth\"",
        "bun harness.ts task:submit --run .capsules/<run-id> --task task-1 --agent worker-1 --token <token> --summary \"Investigated; no code change was needed\" --no-op --reason \"task-0 already fixed the same defect\""
      ]
    },
    {
      "name": "task:validate-start",
      "aliases": [],
      "domain": "task",
      "summary": "Dispatch an independent validator against a submitted task.",
      "description": "Assigns the validator and mints the validation token required by task:review.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Submitted task id."
        },
        {
          "name": "validator",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validator agent id."
        },
        {
          "name": "lease-duration",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Validation window in seconds."
        },
        {
          "name": "validator-domain",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "B12.2 standing checklist domain (code-quality, product, security, system-design, ui-design); binds the matching checklist into this validator's packet. Omitted, the domain is DERIVED from the task's write scope (code-quality always applies; ui-design/system-design follow file extension and path signals) — the first applicable domain nobody has an open validation against yet. A task can carry several open validations at once, one per applicable domain; it reaches validated only once every one of them has passed."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:validate-start --run .capsules/<run-id> --task task-1 --validator val-1",
        "bun harness.ts task:validate-start --run .capsules/<run-id> --task task-1 --validator val-1 --validator-domain code-quality"
      ]
    },
    {
      "name": "task:review",
      "aliases": [],
      "domain": "task",
      "summary": "Record a validator verdict with its gate evidence.",
      "description": "--status pass finalises the task and unblocks dependants; --status fail records a defect finding and returns the task for repair. A failing verdict must carry --summary, --severity and --remediation: they are the validator's own finding and the harness supplies no wording for them. A pass is refused while the task is short of min_adversarial_probes probes, a mandatory gate's recorded run exited non-zero, or an open finding has no --resolve answering it. Every open finding, probe demand or defect, must be answered explicitly: the harness never marks one answered on the validator's behalf. --checklist-domain plus --checklist-report (B12.5) attach standing-checklist coverage to the report: which items were checked and passed, which were not applicable, which could not be checked, and any standing-standard finding outside this task's own scope. None of it gates this task's verdict; the report states it separately so the coverage is visible rather than implied.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Task under validation."
        },
        {
          "name": "validator",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validator agent id."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validation token."
        },
        {
          "name": "status",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "pass or fail."
        },
        {
          "name": "summary",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Verdict summary; with --status fail this is the defect the validator observed and is required."
        },
        {
          "name": "severity",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "critical, important or minor. Required with --status fail; there is no default severity."
        },
        {
          "name": "remediation",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "What would fix the defect. Required with --status fail; the harness writes no remediation of its own."
        },
        {
          "name": "revalidation",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "How the fix is to be proven. Without it the finding cites the task's own gate."
        },
        {
          "name": "evidence",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated command ids proving the verdict."
        },
        {
          "name": "checks",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --evidence."
        },
        {
          "name": "finding-id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Explicit finding id for a failing verdict."
        },
        {
          "name": "requirement",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Requirement a failing verdict binds its finding to."
        },
        {
          "name": "resolve",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Answer an open finding: <finding-id>=<command-id>[,<command-id>]."
        },
        {
          "name": "resolution-method",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "How a finding was answered: <finding-id>=<method>; defaults to the finding's class."
        },
        {
          "name": "checklist-domain",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "B12.5: the standing checklist (code-quality, product, security, system-design, ui-design) this review reports coverage against. Requires --checklist-report; every item in that domain's checklist must be accounted for."
        },
        {
          "name": "checklist-report",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a JSON file: {\"items\":[{\"id\":\"<checklist-id>\",\"disposition\":\"checked|not_applicable|could_not_check\",\"reason\":\"<required unless checked>\"}, ...],\"adjacent_findings\":[{\"id\",\"checklist_item_id\",\"severity\",\"observation\",\"remediation\",\"evidence\":[...]}]}. Requires --checklist-domain."
        },
        {
          "name": "require-semantic-depth",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Enforce strict semantic depth audits on companion manifest criteria and cognitive questions."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:review --run .capsules/<run-id> --task task-1 --validator val-1 --token <token> --status pass --checks C-123 --summary \"All gates pass\"",
        "bun harness.ts task:review --run .capsules/<run-id> --task task-1 --validator val-1 --token <token> --status pass --checks C-123 --resolve probe-task-1-01-1=C-123",
        "bun harness.ts task:review --run .capsules/<run-id> --task task-1 --validator val-1 --token <token> --status fail --summary \"Gate command never ran against the new schema\" --severity critical --remediation \"Point the gate at tests/db and rerun it\"",
        "bun harness.ts task:review --run .capsules/<run-id> --task task-1 --validator val-1 --token <token> --status pass --checks C-123 --summary \"All gates pass\" --checklist-domain code-quality --checklist-report coverage.json"
      ]
    },
    {
      "name": "task:probe",
      "aliases": [],
      "domain": "task",
      "summary": "Record the mandatory adversarial probe: a demand for proof, not a rejection.",
      "description": "Each --demand becomes a probe_demand finding on the task. The task stays in validating under the same validator, repair_round is untouched, and task:review --status pass stays blocked until min_adversarial_probes rounds are recorded and every demand is answered with command evidence.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Task under validation."
        },
        {
          "name": "validator",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validator agent id."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validation token."
        },
        {
          "name": "demand",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "What the implementation must prove; repeat per demand."
        },
        {
          "name": "requirement",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Requirement the demands bind to."
        },
        {
          "name": "revalidation",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "How each demand is to be answered."
        },
        {
          "name": "evidence",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated command ids the demands cite."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:probe --run .capsules/<run-id> --task task-1 --validator val-1 --token <token> --demand \"Prove the parser rejects an empty payload\""
      ]
    },
    {
      "name": "task:reject",
      "aliases": [],
      "domain": "task",
      "summary": "Reject a task with a structured finding for targeted repair.",
      "description": "Records the validator's finding and returns the task to the implementer. The severity and the remediation are the validator's own judgement, so both are demanded; nothing is graded or worded on its behalf.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Task under validation."
        },
        {
          "name": "validator",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validator agent id."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validation token."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "What is defective."
        },
        {
          "name": "severity",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "critical, important or minor."
        },
        {
          "name": "remediation",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "What would fix the defect. Required unless --finding carries it."
        },
        {
          "name": "finding",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --remediation."
        },
        {
          "name": "finding-id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Explicit finding id."
        },
        {
          "name": "evidence",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated command ids proving the defect."
        },
        {
          "name": "checks",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --evidence."
        },
        {
          "name": "requirement",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Requirement the finding binds to."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:reject --run .capsules/<run-id> --task task-1 --validator val-1 --token <token> --reason \"Missing input validation\" --severity critical --remediation \"Validate the payload before the insert\""
      ]
    },
    {
      "name": "task:assign-repairer",
      "aliases": [],
      "domain": "task",
      "summary": "Replace the original implementer as a task's repairer, with a recorded reason.",
      "description": "The original implementer always gets the first repair opportunity; this records the harness's own decision to hand the repair lease to someone else instead. --reason stale requires the prior repair attempt's lease to have gone stale; --reason repeated_failure requires at least two recorded repair rounds; --reason unavailable carries no precondition beyond the task already awaiting its original repairer.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Task in changes_requested, awaiting its original repairer."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is recording the reassignment."
        },
        {
          "name": "repairer",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Replacement agent id; must differ from the original."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "repeated_failure, stale, or unavailable; each carries its own precondition."
        },
        {
          "name": "evidence",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Why the replacement is warranted."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:assign-repairer --run .capsules/<run-id> --task task-1 --actor coordinator --repairer worker-2 --reason unavailable --evidence \"worker-1 released without claiming the repair lease\""
      ]
    },
    {
      "name": "task:abandon",
      "aliases": [],
      "domain": "task",
      "summary": "Close an open attempt nobody submitted or released, on the coordinator's authority.",
      "description": "The forced counterpart to task:release: it does not require the lease token, only --actor and --reason, because it exists for a coordinator to unstick a task whose attempt is open but whose agent is gone or unresponsive. The task returns to retry_ready, or to changes_requested when the abandoned attempt was a repair. Refuses if the task's most recent attempt is already closed - there is nothing left open to abandon.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Task with an open attempt."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is abandoning the attempt. Recorded on the event."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Why the attempt is being abandoned."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:abandon --run .capsules/<run-id> --task task-1 --actor coordinator --reason \"agent-1 crashed mid-attempt and will not return\""
      ]
    },
    {
      "name": "run:exec",
      "aliases": [],
      "domain": "run",
      "summary": "Run a gate command under process isolation and record the evidence.",
      "description": "Captures argv, cwd, timestamps, exit code and log bytes into the capsule, then ingests any screenshots, visual report and browser run metadata the command produced.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Task the command belongs to."
        },
        {
          "name": "gate",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Gate id the command proves."
        },
        {
          "name": "cwd",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Working directory; falls back to the repository root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is running the command. Recorded on the command and its event; there is no default actor."
        },
        {
          "name": "tool-category",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Generic category of the tool, e.g. browser-automation, build, database, documentation, file-edit, formatter, http-client, linter, package-manager, search, shell, test-runner, type-checker, version-control. Any other value is recorded as given."
        },
        {
          "name": "tool",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "The tool this command invoked, named as you name it."
        },
        {
          "name": "tool-extra",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "One tool-specific fact about this command as <key>=<value>, kept verbatim under the reported name."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": true,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - the command ran; read exit_code for the child's own result"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts run:exec --run .capsules/<run-id> --task task-1 --gate gate-1 --actor val-1 --tool-category test-runner --tool bun-test -- bun test tests/unit/auth.test.ts"
      ]
    },
    {
      "name": "run:status",
      "aliases": [],
      "domain": "run",
      "summary": "Show phase, per-task status and progress for the run.",
      "description": "Reads the capsule without mutating it and renders the execution table.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "detailed",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Include the raw state in the JSON result."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts run:status --run .capsules/<run-id>"
      ]
    },
    {
      "name": "run:complete",
      "aliases": [],
      "domain": "run",
      "summary": "Seal the capsule after verifying every completion artifact.",
      "description": "Re-verifies the recorded command evidence and the live repository binding, then commits terminal completion and regenerates the summary suite.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is completing the run. Recorded on the completion event; there is no default actor."
        },
        {
          "name": "auth-token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The token critic:review handed back on approval; verified against the completeness critic's own record before the run can be sealed."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts run:complete --run .capsules/<run-id> --actor coordinator --auth-token <token-from-critic:review>"
      ]
    },
    {
      "name": "critic:start",
      "aliases": [],
      "domain": "critic",
      "summary": "Authorise a completeness critic against the immutable prompt bytes.",
      "description": "Records a repository inspection, assigns the critic, and returns the critic token required to review.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "critic",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Critic agent id."
        },
        {
          "name": "repository-command-ids",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Extra authoritative command ids to add as repository evidence, alongside every run-gate command the harness auto-discovers."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts critic:start --run .capsules/<run-id> --critic critic-1"
      ]
    },
    {
      "name": "critic:review",
      "aliases": [],
      "domain": "critic",
      "summary": "Record the completeness verdict over the whole repository diff.",
      "description": "--decision approve clears completion; request_changes records findings that block it and requires --findings or --findings-file, because the harness never composes a finding on the critic's behalf. Every finding must carry id, requirement_id, severity, observation, remediation and revalidation. Requirement proofs come only from --proofs/--proofs-file or --review; a requirement with no proof is recorded unproven and blocks completion, and a clean verdict with any unproven requirement is refused. integrity_evidence is always the harness's own capsule integrity observation, measured at review time; a --review file cannot certify its own capsule, so whatever it declares under that key is replaced.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "critic",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Critic agent id."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Critic token."
        },
        {
          "name": "decision",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "approve or request_changes."
        },
        {
          "name": "summary",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Verdict summary in the critic's own words."
        },
        {
          "name": "findings",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Inline JSON findings payload."
        },
        {
          "name": "findings-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a JSON findings payload."
        },
        {
          "name": "proofs",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Inline JSON requirement_proofs payload."
        },
        {
          "name": "proofs-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a JSON requirement_proofs payload."
        },
        {
          "name": "review",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a complete review payload."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts critic:review --run .capsules/<run-id> --critic critic-1 --token <token> --decision approve --proofs-file proofs.json --summary \"Whole diff verified\""
      ]
    },
    {
      "name": "critic:reject",
      "aliases": [],
      "domain": "critic",
      "summary": "Reject completion with findings that trigger replanning.",
      "description": "Equivalent to critic:review --decision request_changes with a rejection brief. Structured findings are mandatory: pass --findings or --findings-file.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "critic",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Critic agent id."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Critic token."
        },
        {
          "name": "summary",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Rejection summary in the critic's own words."
        },
        {
          "name": "findings",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Inline JSON findings payload."
        },
        {
          "name": "findings-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a JSON findings payload."
        },
        {
          "name": "proofs",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Inline JSON requirement_proofs payload."
        },
        {
          "name": "proofs-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a JSON requirement_proofs payload."
        },
        {
          "name": "review",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to a complete review payload."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts critic:reject --run .capsules/<run-id> --critic critic-1 --token <token> --summary \"Missing error boundary\" --findings '[{\"id\":\"F-01\",\"requirement_id\":\"req-1\",\"severity\":\"critical\",\"observation\":\"No error boundary around the render tree\",\"remediation\":\"Wrap the tree in an error boundary\",\"revalidation\":\"bun test tests/render\"}]'"
      ]
    },
    {
      "name": "critic:remediate",
      "aliases": [],
      "domain": "critic",
      "summary": "Close out a critic findings review with command-backed remediation evidence.",
      "description": "Every review recorded with status findings stays in history and blocks completion until it carries a remediation naming exactly its own finding ids, each proven by a critic-run, task-unbound, successful command. --resolve is repeatable as <finding-id>=<command-id>[,<command-id>]; --resolution-method names how each finding was closed. --review-sha256 defaults to the currently recorded review.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is recording the remediation."
        },
        {
          "name": "review-sha256",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Digest of the review being remediated."
        },
        {
          "name": "resolve",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Answer a finding: <finding-id>=<command-id>[,<command-id>]."
        },
        {
          "name": "resolution-method",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "How a finding was answered: <finding-id>=<method>."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts critic:remediate --run .capsules/<run-id> --actor coordinator --resolve CF-1=C-fix-1 --resolution-method CF-1=\"focused repair and verification\""
      ]
    },
    {
      "name": "summary:export",
      "aliases": [],
      "domain": "summary",
      "summary": "Write the graph, timeline, metrics and executive brief to disk.",
      "description": "Generates the summary suite under <run>/summary and, with --out, an additional registry export for the graph viewer.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "out",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Directory for the viewer registry export."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts summary:export --run .capsules/<run-id>"
      ]
    },
    {
      "name": "summary:view",
      "aliases": [],
      "domain": "summary",
      "summary": "Render the executive brief without writing anything.",
      "description": "Generates the same suite in memory and returns only the markdown brief.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts summary:view --run .capsules/<run-id>"
      ]
    },
    {
      "name": "finding:get",
      "aliases": [],
      "domain": "inspection",
      "summary": "Read one finding file, or every finding in the capsule.",
      "description": "Without an id the whole findings directory is listed.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Finding id or file name."
        },
        {
          "name": "finding",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --id."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts finding:get --run .capsules/<run-id> --id finding-task-1"
      ]
    },
    {
      "name": "report:get",
      "aliases": [],
      "domain": "inspection",
      "summary": "Read a submission, review or critic report.",
      "description": "With --task the review report is preferred and the submission is used as the fallback; --critic reads the completeness review.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Task whose report is wanted."
        },
        {
          "name": "critic",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Read the critic review report."
        },
        {
          "name": "submission",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Force the submission report."
        },
        {
          "name": "review",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Force the review report."
        },
        {
          "name": "type",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "submission, review or critic."
        },
        {
          "name": "stage",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --type."
        },
        {
          "name": "report",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Explicit report file name."
        },
        {
          "name": "id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --report."
        },
        {
          "name": "screenshots",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Include screenshot records."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts report:get --run .capsules/<run-id> --task task-1 --type review"
      ]
    },
    {
      "name": "evidence:get",
      "aliases": [],
      "domain": "inspection",
      "summary": "Read recorded command evidence.",
      "description": "Filters the evidence directory by command id, task, gate or actor.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "command",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Command id."
        },
        {
          "name": "id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --command."
        },
        {
          "name": "cmd",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --command."
        },
        {
          "name": "task",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Filter by task id."
        },
        {
          "name": "gate",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Filter by gate id."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Filter by actor."
        },
        {
          "name": "screenshots",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Include screenshot records."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts evidence:get --run .capsules/<run-id> --task task-1"
      ]
    },
    {
      "name": "evidence:screenshots",
      "aliases": [],
      "domain": "inspection",
      "summary": "List captured UI screenshots with their test ids and viewports.",
      "description": "Queries the screenshot store rather than the evidence files.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Filter by task id."
        },
        {
          "name": "command",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Filter by command id."
        },
        {
          "name": "cmd",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --command."
        },
        {
          "name": "id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --command."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Filter by actor."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts evidence:screenshots --run .capsules/<run-id> --task task-1"
      ]
    },
    {
      "name": "orchestrator:run",
      "aliases": [
        "orchestrator"
      ],
      "domain": "orchestrator",
      "summary": "Run the autonomous coordination loop over a fresh capsule.",
      "description": "Drives plan, execute, validate and critic rounds until the critic approves or the round budget is spent. The host must inject a round executor; without one the command fails with INVALID_STATE.",
      "flags": [
        {
          "name": "repo",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Repository root; falls back to the current directory."
        },
        {
          "name": "prompt",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Inline prompt text."
        },
        {
          "name": "prompt-file",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "File holding the prompt."
        },
        {
          "name": "prompt-stdin",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Read the prompt from stdin."
        },
        {
          "name": "run-id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Base run id for the generated capsules."
        },
        {
          "name": "run",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Alias of --run-id."
        },
        {
          "name": "capsules-dir",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Directory that holds the capsules."
        },
        {
          "name": "max-rounds",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": 10,
          "description": "Round budget, clamped to 1-10."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded on the loop summary; omitted leaves the loop unattributed."
        }
      ],
      "reads_stdin": true,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts orchestrator:run --repo . --prompt \"Implement the feature\" --max-rounds 3"
      ]
    },
    {
      "name": "orchestrator:supervise",
      "aliases": [],
      "domain": "orchestrator",
      "summary": "Reclaim dead agents, escalate dead-end tasks, and dispatch what's ready (B28).",
      "description": "One reclaim-classify-dispatch pass over a run's eligible set: reclaims leases whose agent died without submitting, escalates tasks whose failures have become deterministic (B28.3) instead of retrying them forever, and reports what is safe to dispatch now versus still backing off. With a host-injected dispatcher it loops until the run reaches a terminal state; without one it performs a single pass, which is what makes it safe to drive from an external poll loop. Recovery is on by default (B28.5) - use --no-recover to disable it. --watch turns this into the poll loop itself: it re-runs the reclaim/escalate heartbeat every --interval seconds until the run goes terminal or the process gets an explicit stop (Ctrl-C / SIGTERM), surfacing changes_requested tasks awaiting a repairer alongside the escalated ones so a rejected task is never silently invisible.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is running the supervisor. Recorded on every event; there is no default actor."
        },
        {
          "name": "max-parallel",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Occupancy ceiling; falls back to the run's configured default."
        },
        {
          "name": "gate-max-parallel",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "B27.2: the separate, lower ceiling for gate-running (CPU-bound) work, reported alongside --max-parallel; falls back to the run's configured default (derived from host cores)."
        },
        {
          "name": "no-recover",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Disable automatic dead-agent reclaim and escalation (on by default)."
        },
        {
          "name": "grace-seconds",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Grace period past lease expiry before reclaiming, 0-86400."
        },
        {
          "name": "poll-interval-ms",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "How often to re-tick while a dispatcher is driving the loop."
        },
        {
          "name": "max-elapsed-ms",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Per-task retry budget before a transient failure reads as deterministic (B28.3)."
        },
        {
          "name": "max-total-elapsed-ms",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Whole-run wall-clock budget before the supervisor stops and reports."
        },
        {
          "name": "deterministic-repeat-threshold",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Consecutive identical failures before they read as deterministic."
        },
        {
          "name": "watch",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Run unattended: re-tick the reclaim/escalate heartbeat every --interval seconds until the run is terminal or the process gets an explicit stop (Ctrl-C / SIGTERM). Ignores any host-injected dispatcher - this is the recovery heartbeat, not a dispatch loop."
        },
        {
          "name": "interval",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": 30,
          "description": "Seconds between heartbeat ticks in --watch mode; refused without --watch."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts orchestrator:supervise --run .capsules/<run-id> --actor coordinator",
        "bun harness.ts orchestrator:supervise --run .capsules/<run-id> --actor coordinator --watch --interval 30"
      ]
    },
    {
      "name": "branch:open",
      "aliases": [],
      "domain": "branch",
      "summary": "Subdivide the work you hold into sub-tasks a sub-agent can take.",
      "description": "A branch is an execution-time subdivision, never a plan task, so it never touches the plan revision. The parent moves to `branched` and its lease clock freezes until collect or abandon, which is what stops a parent blocked on children from being reaped as stale. Every sub-task scope must be a STRICTLY PROPER subset of the parent scope and stay disjoint from its siblings; a violation is refused, not trimmed. That proper-subset rule is what makes a chain of branches terminate. --parent-task accepts a plan task or another branch's sub-task; config max_branch_depth (default 5) is an escalation tripwire on nesting rather than a structural bound, and config max_agents (default 100) caps the grants a run may issue at any depth — a branch is charged one grant per sub-task up front.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "parent-task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Plan task or sub-task the branch hangs off."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent holding the parent lease."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Parent lease bearer token."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Why the work had to be subdivided."
        },
        {
          "name": "sub-task",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Sub-task id; repeat the flag for each sub-task."
        },
        {
          "name": "sub-label",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "`<sub-task-id>=<label>`; one per sub-task."
        },
        {
          "name": "sub-scope",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "`<sub-task-id>=<path>`; repeat for each path."
        },
        {
          "name": "sub-gate",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "`<sub-task-id>=<command>`; optional revalidation gate."
        },
        {
          "name": "repo",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Repository root observed through Git; falls back to the current directory."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Event actor; defaults to the acting agent."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts branch:open --run .capsules/<run-id> --parent-task task-1 --agent worker-1 --token <token> --reason \"parser rewrite blocks the API change\" --sub-task S-1 --sub-label S-1=\"Fix the parser\" --sub-scope S-1=src/one/parser"
      ]
    },
    {
      "name": "branch:claim",
      "aliases": [],
      "domain": "branch",
      "summary": "Lease one branch sub-task to a sub-agent.",
      "description": "Returns the bearer token the sub-agent echoes back to branch:submit. The lease expires like any other, and `recover` reclaims it if the sub-agent dies.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "branch",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Branch id returned by branch:open."
        },
        {
          "name": "sub-task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Sub-task id to claim."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Sub-agent receiving the lease."
        },
        {
          "name": "role",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Branch role the sub-agent works under: sub-implementer, sub-investigator or sub-validator."
        },
        {
          "name": "lease-seconds",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Lease length in seconds (5-86400)."
        },
        {
          "name": "repo",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Repository root observed through Git; falls back to the current directory."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Event actor; defaults to the acting agent."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts branch:claim --run .capsules/<run-id> --branch B-<uuid> --sub-task S-1 --agent sub-1 --role sub-implementer"
      ]
    },
    {
      "name": "branch:submit",
      "aliases": [],
      "domain": "branch",
      "summary": "Hand a finished sub-task back to the branch.",
      "description": "Records what the sub-agent reports it did and releases the sub-lease. The summary is agent-reported; the file-level truth is measured once, by branch:collect.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "branch",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Branch id returned by branch:open."
        },
        {
          "name": "sub-task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Sub-task id being submitted."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Sub-agent holding the sub-lease."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Sub-lease bearer token."
        },
        {
          "name": "summary",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "What the sub-agent changed."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Event actor; defaults to the acting agent."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts branch:submit --run .capsules/<run-id> --branch B-<uuid> --sub-task S-1 --agent sub-1 --token <token> --summary \"Parser accepts the new grammar\""
      ]
    },
    {
      "name": "branch:collect",
      "aliases": [],
      "domain": "branch",
      "summary": "Take the branch back and resume the parent.",
      "description": "Refuses while any sub-task is still live. Records a real Git observation of the worktree delta across the branch window as harness_observed evidence, restores the parent lease with a fresh expiry and returns the parent to `running`. When the repository cannot be observed the file list stays absent rather than becoming an empty one.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "branch",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Branch id returned by branch:open."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Parent agent that opened the branch."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Parent lease bearer token."
        },
        {
          "name": "summary",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "What came back from the sub-agents."
        },
        {
          "name": "repo",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Repository root observed through Git; falls back to the current directory."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Event actor; defaults to the acting agent."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts branch:collect --run .capsules/<run-id> --branch B-<uuid> --agent worker-1 --token <token> --summary \"Parser fixed; API change unblocked\""
      ]
    },
    {
      "name": "branch:abandon",
      "aliases": [],
      "domain": "branch",
      "summary": "Give up on a branch and resume the parent.",
      "description": "The failure path. Every non-terminal sub-task is marked abandoned and its lease released, then the parent gets its lease back and returns to `running` to carry the work itself.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "branch",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Branch id returned by branch:open."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Parent agent that opened the branch."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Parent lease bearer token."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Why the branch is being given up."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Event actor; defaults to the acting agent."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts branch:abandon --run .capsules/<run-id> --branch B-<uuid> --agent worker-1 --token <token> --reason \"sub-agent could not reproduce the failure\""
      ]
    },
    {
      "name": "branch:status",
      "aliases": [],
      "domain": "branch",
      "summary": "Show which branches are open and what they are waiting on.",
      "description": "Lists open branches by default with the reason each one was opened. --all includes collected and abandoned ones, --branch narrows to one and --task narrows to a parent.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "branch",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Show only this branch."
        },
        {
          "name": "task",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Show only branches under this parent."
        },
        {
          "name": "all",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Include collected and abandoned branches."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts branch:status --run .capsules/<run-id>",
        "bun harness.ts branch:status --run .capsules/<run-id> --task task-1 --all"
      ]
    },
    {
      "name": "agent:register",
      "aliases": [],
      "domain": "agent",
      "summary": "Record a dispatched subagent and mint its grant.",
      "description": "Spawning happens host-side; this is how the run learns a subagent exists, who deployed it and under which task. Model, tier, thinking level and toolset below are whatever the dispatcher relays — recorded only when supplied, tagged agent_reported, and left absent otherwise. The harness separately probes the host's own config and transcript for the same fields automatically; only that probe ever earns host_reported/derived/harness_observed. The parent agent must already hold a grant.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent id of the dispatched subagent."
        },
        {
          "name": "role",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Canonical role the agent is granted."
        },
        {
          "name": "host",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Host runtime that spawned the agent."
        },
        {
          "name": "parent-agent",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Agent id that dispatched it; omit for the root."
        },
        {
          "name": "parent-task",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Task or branch sub-task the agent is dispatched onto."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Event actor; defaults to the parent agent, else the agent."
        },
        {
          "name": "provider",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Provider serving the model, as the caller relays it (agent_reported)."
        },
        {
          "name": "model",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Model id as the caller relays it, recorded exactly as given and never parsed (agent_reported)."
        },
        {
          "name": "model-tier",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Tier as the caller relays it: xs, s, m, l or unknown (agent_reported unless unknown)."
        },
        {
          "name": "thinking-level",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Level as the caller relays it: low, medium, high or unknown (agent_reported unless unknown)."
        },
        {
          "name": "context-window",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Context window in tokens, as the caller relays it (agent_reported)."
        },
        {
          "name": "tool",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "One tool as <name> or <name>=<category>; repeat the flag for each tool. Generic category of the tool, e.g. browser-automation, build, database, documentation, file-edit, formatter, http-client, linter, package-manager, search, shell, test-runner, type-checker, version-control. Any other value is recorded as given. A tool given without a category has none recorded."
        },
        {
          "name": "tool-extra",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "One tool-specific fact as <tool>:<key>=<value>, kept verbatim under the reported name. The tool must also be given with --tool."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts agent:register --run .capsules/<run-id> --agent worker-1 --role implementer --host claude-code --parent-agent coordinator-1 --parent-task task-1 --tool Bash=shell --tool-extra Bash:shell=zsh"
      ]
    },
    {
      "name": "agent:report",
      "aliases": [],
      "domain": "agent",
      "summary": "Ingest the caller's own report of tool usage and token counts mid-flight.",
      "description": "Token counts are the caller's running totals and replace the previous ones, tagged agent_reported; --tokens-estimated marks them derived estimates instead. The harness separately probes the host's own transcript for real counts (B34), which is what actually earns harness_observed. At least one of --tool, --tokens-in, --tokens-out or --token-extra is required.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent id holding the grant."
        },
        {
          "name": "tool",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "One tool as <name> or <name>=<category>; repeat the flag for each tool. Generic category of the tool, e.g. browser-automation, build, database, documentation, file-edit, formatter, http-client, linter, package-manager, search, shell, test-runner, type-checker, version-control. Any other value is recorded as given. A tool given without a category has none recorded."
        },
        {
          "name": "tool-extra",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "One tool-specific fact as <tool>:<key>=<value>, kept verbatim under the reported name. The tool must also be given with --tool."
        },
        {
          "name": "tokens-in",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Input tokens consumed so far, as the caller reports it."
        },
        {
          "name": "tokens-out",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Output tokens produced so far, as the caller reports it."
        },
        {
          "name": "token-extra",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "One provider-specific counter as <name>=<count>, kept under the name the caller reported it by."
        },
        {
          "name": "tokens-estimated",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Record the counts as estimates, not measurements."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Event actor; defaults to the reporting agent."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts agent:report --run .capsules/<run-id> --agent worker-1 --tool Read=file-edit --tool Grep=search --tokens-in 18000 --tokens-out 2400 --token-extra cache_read_input_tokens=91000"
      ]
    },
    {
      "name": "agent:release",
      "aliases": [],
      "domain": "agent",
      "summary": "Close a subagent's grant.",
      "description": "Marks the grant released and stamps the release time. A released agent can no longer report.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent id holding the grant."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Why the grant closed."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Event actor; defaults to the released agent."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts agent:release --run .capsules/<run-id> --agent worker-1 --reason \"task-1 submitted\""
      ]
    },
    {
      "name": "agent:list",
      "aliases": [],
      "domain": "agent",
      "summary": "Show who is deployed, or the lineage of one task.",
      "description": "Without flags it lists active grants with whatever telemetry was recorded, each field labelled with the evidence class it actually earned. --task answers who worked a task and under whom, including the agents those agents dispatched.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Report the lineage of this task instead of the roster."
        },
        {
          "name": "all",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Include released grants."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts agent:list --run .capsules/<run-id>",
        "bun harness.ts agent:list --run .capsules/<run-id> --task task-1"
      ]
    },
    {
      "name": "orphan:dispose",
      "aliases": [],
      "domain": "orphan",
      "summary": "Close out a command record that arrived without a live owner.",
      "description": "Orphan evidence — typically a durable command record left behind by an agent that died mid-run — blocks completion until it is explicitly dispositioned. --disposition is ignored_non_authoritative, rejected, or superseded; there is no default, and each disposition is terminal.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is recording the disposition."
        },
        {
          "name": "orphan-sha256",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Digest of the orphan evidence, from doctor's issues."
        },
        {
          "name": "disposition",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "ignored_non_authoritative, rejected, or superseded."
        },
        {
          "name": "rationale",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Why this disposition is correct."
        },
        {
          "name": "evidence",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Command id supporting the disposition; repeat per id."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts orphan:dispose --run .capsules/<run-id> --actor coordinator --orphan-sha256 <sha> --disposition ignored_non_authoritative --rationale \"agent worker-3 died before submitting; the command it ran is not authoritative for any task\" --evidence C-abc123"
      ]
    },
    {
      "name": "authority:decide",
      "aliases": [],
      "domain": "authority",
      "summary": "Grant or decline a needs_authority requirement.",
      "description": "A requirement disposed needs_authority holds every task built on it non-executable until this is recorded. Granting makes it actionable; declining disposes it out_of_scope and cancels every dormant task that depends on it alone, refusing instead if that would invalidate an active or completed one. The decision is permanent: a second call with the same actor and rationale is idempotent, any other call against an already-decided requirement is refused.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "requirement",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Requirement id, currently disposed needs_authority."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is making the decision."
        },
        {
          "name": "decision",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "grant or decline."
        },
        {
          "name": "rationale",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Why this decision is correct."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts authority:decide --run .capsules/<run-id> --requirement req-prod-deploy --actor coordinator --decision grant --rationale \"Human approved the production deploy in the review thread\""
      ]
    },
    {
      "name": "install",
      "aliases": [],
      "domain": "install",
      "summary": "Install the skill release and link it into the requested clients.",
      "description": "Copies the validated source tree to <home>/.agents/skills and publishes a symlink per client, rolling the whole transaction back on failure.",
      "flags": [
        {
          "name": "source",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Skill source directory to install."
        },
        {
          "name": "home",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Home directory that receives the release."
        },
        {
          "name": "clients",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated clients: antigravity, claude, codex, chatgpt."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts install --source . --home ~ --clients claude,antigravity"
      ]
    },
    {
      "name": "installation-status",
      "aliases": [],
      "domain": "install",
      "summary": "Audit the installed release, its digest and its client links.",
      "description": "Compares the installed tree digest against the source, then checks every client symlink target.",
      "flags": [
        {
          "name": "source",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Skill source directory to compare against."
        },
        {
          "name": "home",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Home directory holding the release."
        },
        {
          "name": "clients",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated clients; defaults to the installed manifest."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts installation-status --source . --home ~"
      ]
    },
    {
      "name": "coverage:check",
      "aliases": [],
      "domain": "diagnostics",
      "summary": "Audit repository test coverage against strict 95% threshold.",
      "description": "Runs bun test with coverage collection, parses per-file metrics across lines, statements, functions, and branches, and enforces the minimum 95% threshold.",
      "flags": [
        {
          "name": "threshold",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": "0.95",
          "description": "Minimum coverage threshold fraction, default 0.95."
        },
        {
          "name": "dir",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Target repository directory to run coverage check in."
        },
        {
          "name": "strict",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Exit nonzero when coverage is below threshold."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts coverage:check",
        "bun harness.ts coverage:check --threshold 0.95 --strict"
      ]
    },
    {
      "name": "health",
      "aliases": [],
      "domain": "diagnostics",
      "summary": "Check whether the code still does what the requirements said.",
      "description": "Reports unused exports and unreachable modules, dead or superseded code, declared behaviour nothing enforces, requirements with no code or no test, literal fallbacks that substitute a plausible value for a missing one, and vendor names in identifier positions. Every check prints what it cannot see. Unlike `doctor` it reads a source tree, not a capsule.",
      "flags": [
        {
          "name": "scripts",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Harness scripts root to inspect. Defaults to the running harness."
        },
        {
          "name": "consumer",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Consumer repository root. Without it the vendor-name sweep covers one repo, and says so."
        },
        {
          "name": "check",
          "type": "string",
          "required": false,
          "repeatable": true,
          "default": null,
          "description": "Restrict the run to named checks."
        },
        {
          "name": "all",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "List every failure instead of the first five per check, and every advisory alongside them."
        },
        {
          "name": "strict",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Exit nonzero when the report is unhealthy."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts health",
        "bun harness.ts health --consumer ../gvui --all",
        "bun harness.ts health --check unused-code --strict"
      ]
    },
    {
      "name": "doctor",
      "aliases": [],
      "domain": "diagnostics",
      "summary": "Verify capsule integrity, command evidence and the runtime.",
      "description": "Re-hashes the event chain, re-verifies every recorded command, reports workflow blockers and, with --source and --home, the installation state.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "source",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Skill source directory for the installation check."
        },
        {
          "name": "home",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Home directory for the installation check."
        },
        {
          "name": "clients",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Comma-separated clients for the installation check."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts doctor --run .capsules/<run-id>"
      ]
    },
    {
      "name": "doctor:repair",
      "aliases": [],
      "domain": "diagnostics",
      "summary": "Re-derive state.json from the event chain after a crash tears the log's tail.",
      "description": "The repair counterpart to `doctor`: `doctor` only reports a torn tail or a state/event mismatch. This re-derives state.json from the event chain's last complete event, quarantining any torn final fragment under quarantine/ instead of discarding it, and records a projection-recovered event. Refuses if the manifest or prompt itself is corrupt - that is an integrity failure, not something to repair silently.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is running the repair. Recorded on the event; there is no default actor."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts doctor:repair --run .capsules/<run-id> --actor coordinator"
      ]
    },
    {
      "name": "recover",
      "aliases": [],
      "domain": "diagnostics",
      "summary": "Release expired leases and interrupted validations.",
      "description": "Returns tasks whose lease expired to retry_ready (or changes_requested after a repair attempt), reopens interrupted validations, reclaims branch sub-tasks whose sub-agent died, and expires a stale completeness critic. A branched parent's frozen lease is never reaped: it is blocked on children, not gone.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is running the recovery. Recorded on the event; there is no default actor."
        },
        {
          "name": "grace-seconds",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": 30,
          "description": "Grace period past expiry, 0-86400."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts recover --run .capsules/<run-id> --actor coordinator"
      ]
    },
    {
      "name": "task:release",
      "aliases": [],
      "domain": "task",
      "summary": "Hand a live lease back without waiting for it to expire.",
      "description": "The voluntary counterpart to `recover`. Requires the live lease token; the task returns to retry_ready, or to changes_requested when the released attempt was a repair. A branched task cannot be released - collect or abandon the branch first.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Leased task id."
        },
        {
          "name": "agent",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Agent holding the lease."
        },
        {
          "name": "token",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Lease bearer token."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts task:release --run .capsules/<run-id> --task task-1 --agent worker-1 --token <token>"
      ]
    },
    {
      "name": "worktree:reclaim",
      "aliases": [],
      "domain": "diagnostics",
      "summary": "Free an abandoned run's worktree directories.",
      "description": "B22.6: removes the worktree directories a crashed or abandoned run left behind, after a human has looked and decided the run is not being resumed. The harness branch and every per-task worktree branch are left untouched — only the disposable worktree checkouts are removed. Refuses if worktree isolation is currently off for this run's config.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Who is running the reclaim. Recorded on the event; there is no default actor."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts worktree:reclaim --run .capsules/<run-id> --actor coordinator"
      ]
    },
    {
      "name": "explain",
      "aliases": [],
      "domain": "diagnostics",
      "summary": "Explain a HarnessError code: the rule it enforces, common causes and the remedy for each.",
      "description": "Answers a refused command with a command instead of a file to read. --code is one of the ErrorCode values a HarnessError actually carries (INTEGRITY, INVALID_ARGUMENT, INVALID_STATE, LOCK_TIMEOUT, NOT_IMPLEMENTED, PATH_SAFETY, UNSUPPORTED_PLATFORM); case-insensitive. Every cause is grounded in real throw sites in this build, cited by file and line, plus a live count of how many places in the current source tree still throw that code. --command narrows further: it dynamically scans that command's own implementation file for direct throws of --code and reports the exact lines and messages, rather than a canned guess about which command hits which cause.",
      "flags": [
        {
          "name": "code",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "HarnessError code to explain: INTEGRITY, INVALID_ARGUMENT, INVALID_STATE, LOCK_TIMEOUT, NOT_IMPLEMENTED, PATH_SAFETY, or UNSUPPORTED_PLATFORM. Case-insensitive."
        },
        {
          "name": "command",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "CLI command name (e.g. task:claim) to narrow the explanation to that command's own direct throw sites."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts explain --code INTEGRITY",
        "bun harness.ts explain --code INVALID_STATE --command task:claim"
      ]
    },
    {
      "name": "gate:prove",
      "aliases": [],
      "domain": "gate",
      "summary": "Prove a compiled task's gate can actually fail, on a disposable scratch copy.",
      "description": "Copies the repository's tracked and not-ignored files into a throwaway directory, reverts the task's write scope there back to --base (default HEAD), and runs the task's compiled gate against that reverted copy. Falsifiable means the gate exits non-zero once the task's own work is gone — the property this project's own forensics found missing (docs/planning/coordinator-conformance/FORENSICS.md, DESIGN.md's C3): ten tasks sharing one whole-repo `bun run typecheck` gate that passed whether the task did its work or nothing at all. Only runs post-compile, against a task's already-compiled gate and write scope — at plan:compile time the task's work does not exist yet, so reverting it would yield a scratch copy identical to the current tree, and every verdict would degenerate to 'not falsifiable'; gate:prove is a deliberate later step, not something plan:compile runs for you. The verdict is recorded as a gate-proved capsule event via `appendGateProof`, readable back by graph/plan-audit.ts's `auditPlan` through `latestGateProof` when a caller supplies the run's state: A3-gate-discrimination and A6-whole-suite-gate treat a matching falsifiable:true proof as satisfying the invariant instead of refusing on the static heuristic alone. It never touches the real repository, since every read and write happens inside the scratch copy, deleted before this command returns. Exits 0 whether the verdict is falsifiable or not — a negative verdict is real information for the audit to act on, not a gate:prove failure; only a setup problem (no compiled gate for the task, no Git history to revert against, an unreadable repository) throws.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Compiled task id whose gate is being proved."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded on the event."
        },
        {
          "name": "base",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": "the claimed base sha, else HEAD",
          "description": "Git ref the task's write scope is reverted to before the gate runs. Defaults to the sha task:claim recorded on the task's latest attempt, so the revert lands before that attempt's own commits; falls back to HEAD only when no such sha was recorded."
        },
        {
          "name": "timeout-ms",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Wall-clock budget for the gate command against the scratch copy; default 300000."
        },
        {
          "name": "max-files",
          "type": "int",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Refuses to copy a tree larger than this many tracked/untracked files, so an unexpectedly huge repository fails loudly instead of proving slowly; default 50000."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts gate:prove --run .capsules/<run-id> --task task-1 --actor coordinator",
        "bun harness.ts gate:prove --run .capsules/<run-id> --task task-1 --actor coordinator --base HEAD~1"
      ]
    },
    {
      "name": "coordinator:pushback",
      "aliases": [],
      "domain": "task",
      "summary": "Reject a validator's own recorded pass, procedurally or substantively.",
      "description": "QUEUE-6: the edge every pushback ran on was validator -> implementer; this is the missing coordinator -> validator edge, for when the validator's OWN recorded pass does not hold up. The task must currently be `validated` (every applicable domain passed, not yet finished) and must carry a recorded pass from --validator in --domain, or this refuses. `--cause procedural` means the review act itself did not meet the evidentiary bar (no evidence recorded, a required check skipped) — the implementer's work is not in question, so the task returns only to `validating` for a fresh, properly-evidenced review. `--cause substantive` means the work itself is judged wrong despite the recorded pass — that carries the same consequence a validator's own reject does: repair_round advances, the original implementer is reassigned, and the task goes to `changes_requested` (or `escalated` once repair rounds are exhausted). The disputed pass is archived into validation_history, never silently dropped, and every pushback is recorded on the task under `coordinator_pushbacks` with its cause, so a rejection for 'you did not record what you did' is expressible and auditable, not just implied by a status change.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root."
        },
        {
          "name": "task",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Task carrying the standing pass being contested."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Coordinator agent id recorded as the author of this pushback."
        },
        {
          "name": "validator",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validator whose recorded pass is being pushed back on."
        },
        {
          "name": "domain",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Validator domain the disputed pass covers, e.g. ui-design."
        },
        {
          "name": "cause",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "'procedural' (the review was not properly evidenced) or 'substantive' (the work itself is wrong)."
        },
        {
          "name": "observation",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "What the coordinator found wrong with the pass."
        },
        {
          "name": "remediation",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "What must happen before this can pass again."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts coordinator:pushback --run .capsules/<run-id> --task task-1 --actor coordinator --validator val-1 --domain ui-design --cause procedural --observation \"pass carried zero screenshot evidence\" --remediation \"re-run the visual suite and record real evidence before passing again\"",
        "bun harness.ts coordinator:pushback --run .capsules/<run-id> --task task-1 --actor coordinator --validator val-1 --domain code-quality --cause substantive --observation \"the recorded check output shows the gate never ran\" --remediation \"fix the gate invocation and resubmit\""
      ]
    },
    {
      "name": "capture:init",
      "aliases": [],
      "domain": "capture",
      "summary": "Initialize standard capture configuration in repository.",
      "description": "Generates .capture.yaml or .capture.json with standard presets, default viewports, authentication settings, and example screen targets.",
      "flags": [
        {
          "name": "config-dir",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Directory to create the configuration file in."
        },
        {
          "name": "format",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": "yaml",
          "description": "Configuration format: yaml or json (default: yaml)."
        },
        {
          "name": "preset",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": "standard-dashboard",
          "description": "Preset template: standard-dashboard, marketing-site, mobile-app, full-matrix."
        },
        {
          "name": "force",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Overwrite existing configuration file if present."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts capture:init",
        "bun harness.ts capture:init --format json --preset standard-dashboard"
      ]
    },
    {
      "name": "capture:run",
      "aliases": [],
      "domain": "capture",
      "summary": "Execute multi-viewport UI capture and companion manifest persistence.",
      "description": "Dispatches Playwright or simulated runner across configured screens and viewports, generating screenshots and 1-to-1 companion manifest JSON records.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Capsule run root for artifact and screenshot ledger ingestion."
        },
        {
          "name": "config",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Explicit path to .capture.yaml or .capture.json."
        },
        {
          "name": "config-dir",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Directory containing capture configuration."
        },
        {
          "name": "screen",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Filter execution to a specific screen ID."
        },
        {
          "name": "viewport",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Filter execution to a specific viewport name."
        },
        {
          "name": "out-dir",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Explicit output directory for captures and manifests."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Actor recorded in ledger captures (default: capture-runner)."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts capture:run --config .capture.yaml",
        "bun harness.ts capture:run --run .capsules/<run-id> --screen dashboard --viewport desktop"
      ]
    },
    {
      "name": "capture:eval",
      "aliases": [],
      "domain": "capture",
      "summary": "Evaluate companion manifests against 4-pillar validation engines.",
      "description": "Performs strict binary certification across mechanical, cognitive, custom, and synthesis pillars with 0 numeric scores.",
      "flags": [
        {
          "name": "manifest",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Path to single .manifest.json companion file."
        },
        {
          "name": "manifest-dir",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Directory containing .manifest.json companion files."
        },
        {
          "name": "strict",
          "type": "bool",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Exit non-zero (exit code 3) if any defects are found."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts capture:eval --manifest .captures/dashboard-desktop.manifest.json",
        "bun harness.ts capture:eval --manifest-dir .captures --strict"
      ]
    },
    {
      "name": "mind:init",
      "aliases": [],
      "domain": "mind",
      "summary": "Initialize a mind capsule from an owner charter.",
      "description": "Validates the markdown charter file per CONTRACTS.md §7, creates the mind capsule (mind-gen-<generation>), pins the charter digest into manifest.json, seeds the state projection, and writes the initial last_pulse.json.",
      "flags": [
        {
          "name": "repo",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Repository root the mind serves."
        },
        {
          "name": "charter",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Path to the owner's charter file."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Recorded on mind-initialized."
        },
        {
          "name": "mind-id",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": "mind-gen-1",
          "description": "Mind capsule run id; defaults to mind-gen-1."
        },
        {
          "name": "capsules-dir",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Override .capsules/ directory location."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:init --repo . --charter docs/mind/CHARTER.md --actor owner"
      ]
    },
    {
      "name": "mind:wake",
      "aliases": [],
      "domain": "mind",
      "summary": "Produce the Tier A orientation brief and reclaim expired pulses.",
      "description": "Inspects the mind capsule state and budget, reclaims any open pulse past its deadline via mind-pulse-reclaimed, and outputs the Tier A orientation brief ending in prescribed next actions.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Recorded only if the call reclaims a dead pulse."
        },
        {
          "name": "depth",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": "brief",
          "description": "Orientation depth: brief (default) or run."
        },
        {
          "name": "target-run",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "With --depth run, the run capsule whose handoff to render."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:wake --run .capsules/mind-gen-1"
      ]
    },
    {
      "name": "mind:pulse-open",
      "aliases": [],
      "domain": "mind",
      "summary": "Open an active mind pulse under budget constraints.",
      "description": "Opens a new pulse cycle, validating budget headroom, daily pulse and wall-clock caps, quiet hours, and charter digest consistency before appending mind-pulse-opened.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The tier-0 agent id."
        },
        {
          "name": "host",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Host runtime as reported."
        },
        {
          "name": "driver",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Driver identity as reported."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:pulse-open --run .capsules/mind-gen-1 --actor mind-1 --host antigravity --driver bash-loop"
      ]
    },
    {
      "name": "mind:pulse-close",
      "aliases": [],
      "domain": "mind",
      "summary": "Close an active mind pulse with an outcome, value score, and next-pulse arm.",
      "description": "Closes the open pulse, calculates value delivered, enforces the arming rail (requiring --arm or --terminal-reason), appends mind-pulse-closed, and updates last_pulse.json.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Must match the opening actor."
        },
        {
          "name": "pulse",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Pulse id; must match the open pulse."
        },
        {
          "name": "outcome",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "One of the eleven outcomes in PLAN.md §4.3."
        },
        {
          "name": "arm",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Duration for the next wake, e.g. 15m."
        },
        {
          "name": "arm-mechanism",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "How it was armed, as reported."
        },
        {
          "name": "terminal-reason",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Required when --arm is absent and the outcome is not terminal."
        },
        {
          "name": "witness",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Command id evidencing the work this pulse did."
        },
        {
          "name": "signal",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Typed signal, e.g. rate_limit; never inferred from prose."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:pulse-close --run .capsules/mind-gen-1 --actor mind-1 --pulse pulse-1 --outcome quiescent --arm 15m --arm-mechanism systemd-timer"
      ]
    },
    {
      "name": "mind:observe",
      "aliases": [],
      "domain": "mind",
      "summary": "Record a discovery source scan count evidenced by a command record.",
      "description": "Records an observation from one of the ten discovery sources evidenced by a recorded command id, appending mind-observed.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "source",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "One of the ten source ids in PLAN.md §7.2."
        },
        {
          "name": "command-id",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The recorded command whose output this is."
        },
        {
          "name": "count",
          "type": "int",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "How many items that source returned."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:observe --run .capsules/mind-gen-1 --actor mind-1 --source intent-drift --command-id cmd-41 --count 0"
      ]
    },
    {
      "name": "mind:candidate",
      "aliases": [],
      "domain": "mind",
      "summary": "Record a discovery candidate (defect or proposal).",
      "description": "Proposes a defect or proposal candidate. Defects require a witness command record and falsifier argv. Validates charter goal alignment and write scope before recording mind-candidate-opened.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "kind",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Candidate kind: defect or proposal."
        },
        {
          "name": "statement",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "One line statement, recorded agent_reported."
        },
        {
          "name": "witness",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Command id evidencing the defect; required unless --kind proposal."
        },
        {
          "name": "charter-goal",
          "type": "string",
          "required": true,
          "repeatable": true,
          "default": null,
          "description": "Goal ids from the pinned charter; repeat for multiple."
        },
        {
          "name": "falsifier",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Argv that fails now and would pass if fixed (defects only)."
        },
        {
          "name": "write-scope",
          "type": "string",
          "required": true,
          "repeatable": true,
          "default": null,
          "description": "Paths the work would touch; repeat for multiple."
        },
        {
          "name": "rationale",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Proposals only."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:candidate --run .capsules/mind-gen-1 --actor mind-1 --kind defect --statement \"typecheck fails\" --witness cmd-123 --charter-goal G1 --falsifier \"bun run typecheck\" --write-scope orchestrating-long-tasks/scripts/src/health/"
      ]
    },
    {
      "name": "mind:admit",
      "aliases": [],
      "domain": "mind",
      "summary": "Run admission gates on a candidate and admit it.",
      "description": "Runs the six admission gates (falsifier verification, scope disjointness, charter alignment, etc.) in order and admits the candidate, appending mind-candidate-admitted.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "candidate",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Candidate id."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:admit --run .capsules/mind-gen-1 --actor mind-1 --candidate cand-12"
      ]
    },
    {
      "name": "mind:decline",
      "aliases": [],
      "domain": "mind",
      "summary": "Permanently decline a candidate with a recorded reason.",
      "description": "Marks a candidate permanently declined with a recorded reason and gate failure attribution, appending mind-candidate-declined.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "candidate",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Candidate id."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Reason why candidate was declined."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:decline --run .capsules/mind-gen-1 --actor mind-1 --candidate cand-12 --reason \"scope overlaps active lease\""
      ]
    },
    {
      "name": "mind:quiesce",
      "aliases": [],
      "domain": "mind",
      "summary": "Record a verified quiescent observation across all ten discovery sources.",
      "description": "Records that all ten discovery sources were scanned and found clean with zero items, appending mind-quiesced.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "source",
          "type": "string",
          "required": true,
          "repeatable": true,
          "default": null,
          "description": "Source scan result as <source>:<command-id>:<count>; repeat for each of the ten sources."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:quiesce --run .capsules/mind-gen-1 --actor mind-1 --source intent-drift:cmd-1:0 --source unassigned-todos:cmd-2:0"
      ]
    },
    {
      "name": "mind:escalate",
      "aliases": [],
      "domain": "mind",
      "summary": "Record an escalation and append to escalation log.",
      "description": "Records an escalation event in the hash chain and appends the escalation reason to escalation.md.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Reason for escalation."
        },
        {
          "name": "severity",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Severity of escalation."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:escalate --run .capsules/mind-gen-1 --actor mind-1 --reason \"budget exhausted unexpectedly\""
      ]
    },
    {
      "name": "mind:halt",
      "aliases": [],
      "domain": "mind",
      "summary": "Halt mind pulse execution and suppress successor arming.",
      "description": "Halts the mind run, suppresses further autonomous pulse arming, records mind-halted, and updates last_pulse.json with next_wake_at set to null.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "reason",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Reason for halting."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:halt --run .capsules/mind-gen-1 --actor mind-1 --reason \"critical safety check failure\""
      ]
    },
    {
      "name": "mind:round-open",
      "aliases": [],
      "domain": "mind",
      "summary": "Open a multi-pulse round for an objective.",
      "description": "Opens a new execution round for an objective in Phase 4, linking the round to its target capsule and appending mind-round-opened.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "objective",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Objective id."
        },
        {
          "name": "round",
          "type": "int",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Round index."
        },
        {
          "name": "target-run",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Chained-from capsule run id."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:round-open --run .capsules/mind-gen-1 --actor mind-1 --objective obj-1 --round 1"
      ]
    },
    {
      "name": "mind:round-close",
      "aliases": [],
      "domain": "mind",
      "summary": "Close a multi-pulse round for an objective.",
      "description": "Closes an active execution round for an objective in Phase 4, recording successor objective or terminal reason, appending mind-round-closed.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent."
        },
        {
          "name": "objective",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Objective id."
        },
        {
          "name": "round",
          "type": "int",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Round index."
        },
        {
          "name": "terminal-reason",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Reason if round terminates without successor."
        },
        {
          "name": "successor-run",
          "type": "string",
          "required": false,
          "repeatable": false,
          "default": null,
          "description": "Successor capsule run id."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:round-close --run .capsules/mind-gen-1 --actor mind-1 --objective obj-1 --round 1 --terminal-reason \"objective completed\""
      ]
    },
    {
      "name": "mind:audit-start",
      "aliases": [],
      "domain": "mind",
      "summary": "Start an independent audit cycle over recent pulses.",
      "description": "Initiates an independent audit cycle in Phase 5, recording window start time and auditor identity, appending mind-audit-started.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Auditor agent id."
        },
        {
          "name": "audit-id",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Audit id."
        },
        {
          "name": "window-start",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Window start timestamp (ISO8601)."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:audit-start --run .capsules/mind-gen-1 --actor auditor-1 --audit-id audit-1 --window-start 2026-08-21T00:00:00Z"
      ]
    },
    {
      "name": "mind:audit-report",
      "aliases": [],
      "domain": "mind",
      "summary": "Submit findings and verdict for an audit cycle.",
      "description": "Records the eight audit answers with supporting command ids and overall verdict in Phase 5, appending mind-audit-reported.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The mind capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Auditor agent id."
        },
        {
          "name": "audit-id",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Audit id."
        },
        {
          "name": "verdict",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Audit verdict: approved or failed."
        },
        {
          "name": "answer",
          "type": "string",
          "required": true,
          "repeatable": true,
          "default": null,
          "description": "One of eight audit question answers as <question-id>:<command-id>:<verdict>; repeat for all eight."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:audit-report --run .capsules/mind-gen-1 --actor auditor-1 --audit-id audit-1 --verdict approved --answer Q1:cmd-10:pass"
      ]
    },
    {
      "name": "mind:rotate",
      "aliases": [],
      "domain": "mind",
      "summary": "Rotate generation N capsule into generation N+1.",
      "description": "Performs generational rotation, carrying forward charter pin and declined candidates while preserving auditability.",
      "flags": [
        {
          "name": "run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The current generation capsule root."
        },
        {
          "name": "next-run",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "The next generation capsule root."
        },
        {
          "name": "actor",
          "type": "string",
          "required": true,
          "repeatable": false,
          "default": null,
          "description": "Acting agent id."
        }
      ],
      "reads_stdin": false,
      "takes_remainder": false,
      "exit_codes": [
        {
          "code": 0,
          "meaning": "SUCCESS - markdown brief on stdout, or JSON when --format json is set"
        },
        {
          "code": 3,
          "meaning": "INVALID_ARGUMENT / INVALID_STATE / INTEGRITY / PATH_SAFETY / UNSUPPORTED_PLATFORM - rejected before the capsule changed"
        },
        {
          "code": 4,
          "meaning": "LOCK_TIMEOUT - the capsule lock was still held at the deadline"
        },
        {
          "code": 70,
          "meaning": "NOT_IMPLEMENTED, or an unexpected failure the harness did not classify"
        }
      ],
      "examples": [
        "bun harness.ts mind:rotate --run .capsules/mind-gen-1 --next-run .capsules/mind-gen-2 --actor coordinator-1"
      ]
    }
  ]
}
