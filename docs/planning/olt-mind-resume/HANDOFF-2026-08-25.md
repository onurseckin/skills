# OLT Mind Fleet — Handoff, 2026-08-25

**Status:** fleet terminated mid-flight by an API session limit at ~14:15Z. Every agent
died at once. Nothing was corrupted; nothing was pushed.

**Repo state at handoff:** `HEAD = cd7bf43a`, **9 commits ahead of `origin/main`**, 21
modified + 5 untracked files in the working tree.

---

## 1. FIRST TASK ON RESUME — QUOTA EXHAUSTION IS UNDETECTED

This is the owner's explicit instruction and it outranks everything else in this document.

The fleet did not degrade as it approached the session limit. It ran at full concurrency
until the API refused, and then **every agent died simultaneously** — a Mind, an auditor,
two orchestrators, a planner, and four implementers, several mid-write. Work in flight was
lost rather than wrapped up.

### What exists today

|                                     |                                                              |
| ----------------------------------- | ------------------------------------------------------------ |
| `telemetry/circuit-breaker.ts:14`   | `DEFAULT_QUOTA_THRESHOLD = 5.0`                              |
| `cli/commands/quota-check.ts:20,37` | hardcodes `5.0` twice, bypassing the constant                |
| Consumers of `quota:check`          | **the CLI registry only**                                    |
| Automatic invocation                | **none — nothing calls it on a schedule or before dispatch** |

`quota:check` is a command a human can run. It is not a control. No pulse, no dispatch
path, and no gate consults it, so nothing was ever going to notice the limit approaching.

### Required behaviour

1. **Threshold 10%, config-driven** — from `.olt/policy.json` via the layered resolver in
   `core/config/harness-config.ts`, not a literal. The `5.0` at `quota-check.ts:20,37`
   must read the constant at minimum, and preferably the config.
2. **Wrap-up-not-start protocol at threshold.** On crossing: finish and submit in-flight
   work, stop dispatching new lanes, flush evidence, then quiesce. Do not begin anything
   whose expected cost exceeds the remaining budget.
3. **Automatic evaluation.** Consulted at every Mind pulse and before every dispatch —
   a check nobody runs is not a check. This is the same defect class as the rest of this
   document: _built-but-unwired_.
4. **Fail-closed.** If usage cannot be read, treat it as _unknown_, not as _plenty_.
   Absence must not render as headroom.

**Acceptance test:** simulate 8% remaining and prove the fleet stops dispatching and
wraps up, rather than continuing until the API refuses.

---

## 2. DELIVERED — 9 COMMITS, UNPUSHED

```
cd7bf43a fix(sync): deploy olt/ from HEAD and refuse dirty source trees
c170d68c feat(olt): add config provenance with absent and unreadable tags
c4c089c9 feat(olt): add structural run-identity predicate to grant bootstrap
c337196f fix(olt): remove residual Fast-Path grants and hardcoded cadence
6de8a484 feat(olt): grant coordinator task:abandon authority
16636ad4 fix(olt): remove Fast-Path Compaction false permission
7127a646 feat(olt): add fail-closed actor attribution resolver
f6079d51 fix: scope lefthook lint and format checks to staged files
d18d3874 test(olt): separate grant bootstrap allowlist data tests
```

All hooks green, no `--no-verify`, no AI attribution trailers, no foreign paths.
**Not pushed** — `pre-push` runs `bun run sync`, which was barred all session.

---

## 3. IN FLIGHT WHEN THE FLEET DIED

**cand-37 — in-lease repair attempt fix.** Graph revision 3 approved. Implementation
uncommitted: `workflow/review/micro-cycle.ts` (+80), `tests/unit/workflow/micro-cycle.test.ts`,
`tests/unit/cli/task-reject-repair.test.ts` — 117 insertions total. Its last report was
`task:check` passing and a test gate about to re-run. **Closest to completion; resume here.**

The defect it fixes: `task:reject --in-lease` returns a task to `leased` **without opening
a repair attempt**, so if the implementer then dies the task is neither `submitted` (no
validator can start) nor holding an open attempt (`task:abandon` refuses). Four capsules
are wedged this way.

**cand-4 — fail-closed command authority.** The atomic flip is built and deliberately
held uncommitted in `packets/command-authority.ts`, with
`tests/unit/packets/command-authority-fail-closed.test.ts` untracked. **Do not land it
until:** the three `commands: []` roles are resolved, the `ui-validator`/`validator`
collision is fixed, and the 8 recovery commands are granted to at least one role — or the
flip bricks them.

**cand-1 — manifest governance.** Five `olt/agents/*.yaml` modified. **Incomplete in a
dangerous way:** the top-level `role:` field was corrected while the `instructions:` block
— the text `agent:brief` emits verbatim, which is what agents actually read and register
under — still names the old role. `ui-validator.yaml` contradicts itself four times.
Before this round each file was consistent-and-wrong; now each is wrong in the layer that
drives behaviour while looking correct in the layer an auditor checks first.

---

## 4. SAFETY ITEM — UNREVIEWED CODE IS LIVE IN THE GLOBAL HARNESS

Five files in `~/.agents/skills/olt/scripts/src/` match the **working tree**, not `HEAD`:

```
capture/runners/live-capture-runner.ts
validation/cross-channel-consistency.ts     <- these three were REJECTED by their validator
validation/dual-channel-analyzer.ts            ("redesign, not a patch")
cli/commands/mind-audit-live.ts             <- these two are work whose lease was never closed
mind/cognitive-auditors.ts
```

Deployed ~06:36–08:24Z by a sync that ran before the prohibition existed. Proven by
ordering: global `grants.ts` mtime `06:36:35Z` against the commit introducing that code at
`06:59:08Z` — the harness held it 22 minutes before it was committed.

**Bounded:** `task:check` does not import any of them. `task:review` imports `isUiScope`
from `dual-channel-analyzer.ts`, but that symbol is byte-identical to HEAD, so no verdict
issued today came from unreviewed code. **The isolation is incidental, not structural** —
any future edit to that file lands in the verdict path.

**`packets/command-authority.ts` is clean and equals HEAD**; the deny-by-default flip is
**not** deployed. Verified four times by three agents.

**Remedy:** now that `cd7bf43a` makes sync deploy from HEAD and refuse a dirty tree, a
restore is safe and deliberate. Restore those five from HEAD, verify by content hash.
Requires an owner-seat action; no agent role holds it.

---

## 5. THE OWNER'S OLDEST OPEN DIRECTIVE — OVERSIZED CAPSULE FILES

Reported at the start of the engagement as causing thread errors in other agents. **Still
open, and six times worse than when reported.**

```
7.23 MB  mind-gen-4/events.jsonl                     <- growing at termination
1.42 MB  cand-37-in-lease-repair-attempt-r1b/events.jsonl
1.13 MB  host-parity-hygiene-r2/brainstorming.json   <- the file originally reported
1.13 MB  .../defect-brainstorm-root-escape/stray-brainstorming.json   <- byte-identical duplicate
                                                        in a root-escape path
total .olt/capsules: 221.8 MB
```

**Root cause measured:** `mind-gen-4/events.jsonl` holds **167 events in 7.23 MB** —
about 43 KB per event, of which **`projection_patch` is 88%**. The log stores a full state
projection per event rather than a delta. That is the mechanism to fix; deleting files
treats the symptom.

The stray duplicate is also evidence for the path-escape candidates — **move, never delete.**

---

## 6. DEFECT REGISTER

Every item below was measured, not inferred. Most cannot be filed as candidates because
the proposal queue is capped and the grant path is circular (see §7).

### Vacuous or mis-scoped gates

| Defect                                                          | Evidence                                                                                                                                                                                                                                                                                                                           |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bun run typecheck` cannot see any test file                    | `tsconfig.json` excludes `tests` and never includes it — **0 test files in a 947-file program**. Used by both the sealed completion gate and the `lefthook` pre-commit hook, so fixing either leaves the other blind. Adding `tests` to `include` surfaces **1017 errors across 249 files** — its own candidate, not a glob tweak. |
| `bun test` bare filters OR-combine                              | `bun test real.ts missing.ts` → **exit 0**. A missing file is silently absorbed when any argument matches. `./`-prefixing does **not** fix it (measured, still exit 0); only sole-argument isolation or a `test -f` guard does.                                                                                                    |
| `pre-push` deploys the working tree while `push` publishes HEAD | Two different artifacts on one trigger. Fixed by `cd7bf43a`.                                                                                                                                                                                                                                                                       |
| Whole-tree `format:check` in pre-commit                         | Fixed by `f6079d51`. Its absence is why one lane used `--no-verify`.                                                                                                                                                                                                                                                               |
| `vendor_leak` vs `oxfmt` are mutually unsatisfiable             | `oxfmt` bares every valid-identifier object key; `vendor_leak` flags exactly those; `oxfmt` runs in pre-commit. **No agent can satisfy both** — the rule must exempt object keys.                                                                                                                                                  |
| `must-not-states-tier2-only` matches across the whole array     | Forced an implementer to reword a true, unrelated rule. Fix: assert the structural grant (`permissions.spawns`), not prose.                                                                                                                                                                                                        |
| UI classifier fires on filename                                 | `task:review` demanded a screenshot for a YAML edit because the file was named `ui-validator.yaml`. Blocks both pass and fail.                                                                                                                                                                                                     |

### Absence rendering as presence

| Defect                                                         | Evidence                                                                                                                                                                                                                                                              |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `loadRepoPolicy` swallows the diagnosis                        | `catch { return generateDefaultRepoPolicy() }` — discards what `inspectRepoPolicy` preserves 30 lines above.                                                                                                                                                          |
| `agent-brief.ts:11` reads a path that does not exist           | Every role brief ever issued printed `(None)` for a failed read. **The flagship demonstration of the invariant, still undelivered.**                                                                                                                                  |
| `manifest-schema.ts:77` coerces absent `commands` to `[]`      | Six host manifests become indistinguishable from deliberate zero-grants. Must land **before** cand-4's flip.                                                                                                                                                          |
| `String(doc.role \|\| doc.name \|\| "")`                       | Six manifests parse to the **empty-string role** — a real, matchable identity nobody named.                                                                                                                                                                           |
| A raw `0x00` byte silences grep                                | `health/allowlist.ts` is **committed** with one. Every grep against it has returned a false negative. Gate must target `0x00` specifically, never "`file` reports data" (a PNG fixture false-positives). Convert to the `�` escape — same runtime, readable by tools. |
| Truncated lint output advertises `--all`, which does not exist | `line-limiter.ts:11` is generic and emits the hint without knowing which command it decorates. Decomposing from the truncated view gives 2 violations where the truth is 5.                                                                                           |
| Lint counts double-report                                      | Same rule/file/line/column emitted twice. Any comparison must de-duplicate on `(rule, file, line, column)` and state its granularity.                                                                                                                                 |

### Built-but-unwired _(the inverse pattern — looks absent in the data, present in the source)_

| Defect                                                           | Evidence                                                                                                                                                                       |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `canonicalizeHostId` has **zero callers** on any write path      | `core/config/host-canon.ts` already maps `claude`→`claude-code` and classes `initialization`/`custom` as known-unresolvable. The normaliser everyone specified already exists. |
| `host_address` writer is gated on an optional flag nobody passes | `grants.ts:173` omits the key entirely when unsupplied — **absence leaves no trace**.                                                                                          |
| `task:abandon` committed but not deployed                        | Granted in repo `coordinator.yaml`, absent from the global tree the harness reads.                                                                                             |

### Identity and state

| Defect                                                                    | Evidence                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agent_id` collides by construction                                       | Role-ordinal names minted per run. **8 different `orchestrator-1`s, 4 different `mind-1`s in this repo alone.** A bare-id join does not fail — it **merges**. Sound key is **`(repo, run_id, agent_id)`** — validated at 485 pairs, 0 duplicates.                                                                               |
| `host_address` is the only globally unique agent id, at **1.6% coverage** | 485 capsule agent records, 37 populated, 8 conforming. Fix order is **binding**: normalise `host` → per-host format validator via the adapter → _then_ require the flag. Any other order yields a field always populated and still unjoinable — worse than absent, because it looks fixed.                                      |
| `host` field is overloaded                                                | Carries both which host runs the agent (`claude-code`, `antigravity`, `codex`) and which code path wrote the record (`initialization`, `cli`, `local`). Four spellings of Claude Code across 503 records. **Do not "reject non-hosts"** — `initialization` is written deliberately by `mind:init` for every Mind's boot record. |
| Six hardcoded dispatchable-status sets under four names                   | `DISPATCHABLE`, `DISPATCHABLE_STATUSES` ×2, `candidateStatuses`, one anonymous inline, plus five ad-hoc inline tests. Every advisory set admits `proposed`, which `claim.ts:40` refuses. Repair is routed by a **separate** role-rule mechanism (D2/D3), so the fix must reconcile two mechanisms, not deduplicate one.         |
| `isMechanicValidatorRole` defined **four times**, already divergent       | Three include prefix/suffix matching; `core/contracts/packets.ts:53` does exact match only. Importers split across the divergence, and the authority chokepoint uses the **more permissive** copy.                                                                                                                              |

### Governance mechanism

| Defect                                                         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `authority:decide` granted to **0 of 22 roles**                | And it is the command that _confers_ authority — a bootstrap paradox. It is **not** a candidate for the grant-bootstrap allowlist; that would let any unregistered actor grant itself anything. Correct fix: a dedicated `owner.yaml` whose entire grant is `agent:register` + `authority:decide`. **28 manifests exist and none is an owner**, while the harness's own refusal text names an "owner authority decision". |
| `mind:escalate` has no `resolve` verb                          | 50 escalations filed, **0 resolved**. A mechanism named "escalate" that never raises anything is a fail-open that looks discharged because a record exists.                                                                                                                                                                                                                                                               |
| No command amends a candidate's write scope                    | 54-command surface, no amend/revise/scope-extend. The only post-hoc mutation is `decline`.                                                                                                                                                                                                                                                                                                                                |
| No command amends a sealed completion gate                     | So a gate that later proves non-covering cannot be corrected within its own run. Validation must happen **at `plan:compile`**.                                                                                                                                                                                                                                                                                            |
| The 8 tokenless recovery commands are granted to 0 roles       | `task:reject` is granted to 9. **The recovery surface is not uniformly missing — specifically the _forced_ exits are.** Every stuck capsule is stuck exactly where the holder is gone.                                                                                                                                                                                                                                    |
| Peer messaging by role name resolves for nobody                | Six lost reports today. **Raw agentIds work.** Every brief must carry the recipient's parent and sibling agentIds.                                                                                                                                                                                                                                                                                                        |
| Evidence files carry no currency marker                        | **94 evidence files, 4 marked.** A superseded file and a live one are byte-indistinguishable to a reader arriving by grep. Rule: `SUPERSEDED BY <file>` or `CURRENT AS OF <ts>` in the first five lines, **prepended** — a footnote at the end of a 12,000-line file is not a control.                                                                                                                                    |
| `plan:review` records are immutable _and_ embed specifications | Correct for attestation, wrong for specs. PV-3/PV-4 permanently carries a remediation we proved wrong. Fix: reviews should **reference** a markable spec file, not embed one.                                                                                                                                                                                                                                             |

---

## 7. WHY MOST OF THIS IS UNFILED

The candidate pipeline is circularly blocked:

```
admit a candidate  ->  needs authority:decide
authority:decide   ->  granted to no role; needs owner.yaml
owner.yaml         ->  needs deploying to the tree the harness reads
deploy             ->  needs bun run sync
```

`cd7bf43a` fixed the last link. **Unblocking the chain is the second task on resume**,
after quota detection: land `owner.yaml`, sync from HEAD, then record the pending
decisions (cand-34, cand-36, cand-37 were all ruled GRANT and never recorded).

Until then, `.olt/capsules/cand-11-config-foundation-r1/evidence/r2-inheritance-and-lint-exemptions.md`
(1949 lines) is the **sole record** of several findings above. It is unmarked and
uncommitted.

---

## 8. METHOD NOTES WORTH KEEPING

Fifteen distinct instances of one shape today, and **nothing ever threw**. A NUL byte
silenced grep. A typecheck certified files it could not see. A partial `git add -N` exits 0.
An empty glob passes `--check` trivially.

Four false negatives were caught by one technique — **prove your instrument can still fail
before trusting it to pass** — and three of the four were in _supervisory_ tooling. That
technique originated in the implementer tier and was adopted upward.

Rules derived, in order of usefulness:

1. **Control probe.** Before trusting an empty result, confirm the instrument finds
   something it should.
2. **Measure the source before sizing a fix from the data.** Three "missing" capabilities
   today were built-but-unwired.
3. **A count carries its population and its instant**, or it cannot be compared to another
   count. Three seats produced three correct answers to three different questions and spent
   effort deciding who was wrong. Nobody was.
4. **Prove key uniqueness over the key you wrote down**, not the key you happened to run.
   An instrument correct for reasons its specification does not capture is a latent defect.
5. **Scope a repair to the invariant, not to the instances you spotted.** Every enumerated
   brief produced a partial fix; the one that stated a property produced a complete one and
   closed an unrelated hole as a side effect.
6. **A deferral whose trigger will not fire, or fires exactly when the fix becomes
   expensive, is a decision wearing a schedule.**

---

## 9. RESUME CHECKLIST

1. **Quota detection** (§1) — before redeploying the fleet, or this recurs.
2. **Unblock the candidate pipeline** (§7) — `owner.yaml`, sync from HEAD, record the
   three pending GRANT rulings.
3. **Restore the five unreviewed global files** (§4) — owner-seat action, now safe.
4. **Finish cand-37** (§3) — closest to done; unwedges four capsules.
5. **`events.jsonl` projection_patch** (§5) — the owner's oldest open directive.
6. **cand-1's SECTION-4 manifest repair** (§3) — currently worse than before the round.
7. Then cand-4's flip, but only after its three preconditions.

**Do not push** until the five global files are restored and the manifests are coherent.
`pre-push` runs sync.
