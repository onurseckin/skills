/**
 * Runner Receipt Subdomain Test Facade.
 * Explicit named exports for output pumps, receipt verification, and evidence boundaries.
 */

export {
  boundedEvidenceError,
  MAX_COMMAND_ATTEMPT_BYTES,
  MAX_COMMAND_RECORD_BYTES,
  MAX_COMMAND_INTENT_BYTES,
} from "../../../olt/scripts/src/engine/runner/models/index.ts";
